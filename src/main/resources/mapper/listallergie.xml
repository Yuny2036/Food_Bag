<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dr.foody.listAllergie.dao.ListAllergieDao">

    <select id="getList" parameterType="dr.foody.listAllergie.dto.ListAllergieDto"
            resultType="dr.foody.listAllergie.dto.ListAllergieDto">
        SELECT
        idx,
        code,
        name
        FROM listallergie
        <where>
            <if test="idx !=null">idx = #{idx}</if>
            <if test="code !=null">AND code = #{code}</if>
            <if test="name !=null">AND name = #{name}</if>
        </where>
    </select>
    <update id="mod" parameterType="dr.foody.listAllergie.dto.ListAllergieDto">
        UPDATE listallergie
        <set>
            <if test="code !=null"> code = #{code}</if>
            <if test="name !=null">, name = #{name}</if>
        </set>
        WHERE IDX = #{idx}
    </update>
    <insert id="reg" parameterType="dr.foody.listAllergie.dto.ListAllergieDto"
            useGeneratedKeys="true" keyProperty="idx">
        INSERT listallergie
        (
        code,
        name,
        ) VALUES (
        #{code},
        #{name},
        )
    </insert>
    <delete id="del" parameterType="java.lang.Integer">
        DELETE FROM listallergie WHERE IDX = #{idx}
    </delete>
</mapper>